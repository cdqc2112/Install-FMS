{{ $serv_id := "keycloak-postgresql" }}
apiVersion: v1
kind: Service
metadata:
  name: {{$serv_id}}
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/service: {{$serv_id}}
  ports:
    - name: https
      port: 5432
      targetPort: 5432
      protocol: TCP